name: Build and Test

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Golang CI Lint
    uses: burnt-labs/xion/.github/workflows/golangci-lint.yml@fix/docker_pull_error
    secrets: inherit

  update-swagger:
    name: Update Swagger
    uses: burnt-labs/xion/.github/workflows/update-swagger.yml@fix/docker_pull_error
    secrets: inherit

  unit-tests:
    name: Go Unit Tests
    uses: burnt-labs/xion/.github/workflows/tests.yml@fix/docker_pull_error
    secrets: inherit

  build-docker:
    name: Build Docker Images
    uses: burnt-labs/xion/.github/workflows/docker-build.yml@fix/docker_pull_error
    secrets: inherit

  docker-scout:
    needs: build-docker
    name: Docker Scout
    uses: burnt-labs/xion/.github/workflows/docker-scout.yml@fix/docker_pull_error
    secrets: inherit

  build-heighliner:
    needs: build-docker
    name: Heighliner Build
    uses: burnt-labs/xion/.github/workflows/heighliner.yml@fix/docker_pull_error
    secrets: inherit

  interchain-tests:
    needs: build-docker
    name: Interchain tests
    uses: burnt-labs/xion/.github/workflows/integration-tests.yml@fix/docker_pull_error
    secrets: inherit