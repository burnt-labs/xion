name: Trigger Assets

on:
  workflow_call: # is called from the create-release workflow
  workflow_dispatch: # manual trigger

jobs:
  trigger-assets:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger xion-assets workflow
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_DISPATCH_TOKEN }}
          repository: burnt-labs/xion-assets
          event-type: xion-assets-release-trigger # NOTICE: must match the trigger in xion-assets workflow
          client-payload: |
            {
              "tag_name": "${{ github.event.release.tag_name }}",
              "release_name": "${{ github.event.release.name }}"
            }

